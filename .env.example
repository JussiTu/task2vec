# Copy this to .env and fill in your values
OPENAI_API_KEY=sk-...

# Optional: switch to Voyage AI for embeddings (better for semantic tasks)
# VOYAGE_API_KEY=pa-...
# EMBEDDING_PROVIDER=voyage
# EMBEDDING_MODEL=voyage-3-large

# Defaults (OpenAI)
EMBEDDING_PROVIDER=openai
EMBEDDING_MODEL=text-embedding-3-large

# Paths
JIRA_DUMP_PATH=jira_1000.jsonl
CACHE_DIR=.cache
DB_PATH=.cache/tickets.db
